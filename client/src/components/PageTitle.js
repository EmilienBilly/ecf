const PageTitle = ({ title }) => {
    return (
        <div className="">
            <h1 className="block text-2xl sm:text-3xl font-bold text-white-text my-6">{title}</h1>
        </div>
    );
};

export default PageTitle;
